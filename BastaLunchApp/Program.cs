using System;

namespace BastaLunchApp
{
    class Program
    {
        static void Main(string[] args)
        {
            string msg = "Anzahl Parameter" ;
            Console.WriteLine("Hello World!" + msg);
            for (int i = 0; i < 101; i++)
            {
                Console.WriteLine(i.ToString());

            }
            Console.ReadKey();
        }
    }
}
